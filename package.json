{
  "name": "inhouse-portal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "事務所内+委託協力者向けポータル (GASツールリンク集 / Cloudflare Pages + Functions)",
  "scripts": {
    "dev": "npm run build && wrangler pages dev dist/client --compatibility-date=2026-06-01",
    "dev:web": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "deploy": "npm run build && wrangler pages deploy dist/client --project-name inhouse-portal"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.7",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
